<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="fm.jiecao.jiecaovideoplayer">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".ApplicationDemo"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActiivtyMain"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ActivityDirectlyPlay"/>

        <activity android:name=".ActivityList"/>
        <activity android:name=".ActivityListNormal"/>
        <activity android:name=".ActivityListViewpager"/>
        <activity android:name=".ActivityListMultiHolder"/>

        <activity android:name=".ActivityUi"/>
    </application>

</manifest>
